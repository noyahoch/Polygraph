# Authorized seed-17/27 replication: September 16

**Completed before the frozen cutoffs at 2026-09-16 10:03:09 Israel time.**
The [final results](RESULTS_20260916.md) and [Hebrew summary](REPORT_HE_20260916.md)
record the verified outcome; the launch chronology below is retained.
The user explicitly
requested "run those 2 more seeds" at 2026-09-16 07:26:36 Israel time, following
the proposed 50 GPU-hour ceiling and prospective cutoffs below.

This is the execution of the [fixed replication protocol](PROTOCOL.md), not
another implementation-test run. The prior [89-test validation](VALIDATION.md)
is separate evidence. The original seed-7 run remains a late diagnostic.

## Immutable identity

- Run ID: `layer_ensemble_replication_20260916_072636`.
- Workflow SHA-256: `d7f94fb3c0f04bb537c526a2132f5cfd176affa60b20c3246fa7addec2a2c647`.
- Authorization SHA-256: `33f55af5f3ab1e0425f3a4c283a31175e058388b1244216976f7295da9b09781`.
- Release inventory SHA-256 (workflow serialization): `b7aafc64ee86692e3956b10073ef638bf1965cc084a995b58d8b34a372b19596`.
- Code release: `validated-c9a6da4ff60d`, copied byte-for-byte from the passing
  CPU-897383 source snapshot, with 120 inventoried source-text files.
- Validation inventory SHA-256 (validation serialization):
  `c9a6da4ff60d844b22b9c64014aba578765e36e6c9f8da03a9dde43a9f616db9`.

The two inventory hashes use their respective recorded serializations; they
are not a claim that one serialization has two hashes. The executed source
snapshot is authoritative. Later local documentation commits are separate
from that snapshot; no unpublished working-tree source is required on the server.

Scientific root:

`/home/yandex/MLWG2026/omrifahn/polygraph_september_2026/experiments/layer_ensemble_replication_20260916_072636`

Control root:

`/home/yandex/MLWG2026/omrifahn/polygraph_september_2026/experiments/layer_ensemble_replication_20260916_072636_control`

The control root contains the immutable workflow, authorization, user-approval
receipt, release, logs and durable submission intents. It is separate from the
new scientific root and from the unchanged historical September 14 experiment.

## Accepted Slurm graph

Submission completed at **2026-09-16 07:35:23 Israel time**. These are durable
job IDs, not a live status table:

| Stage | Slurm ID | Purpose |
| --- | --- | --- |
| Guardian | 897592 | Independent status, deadlines and terminal evidence |
| Prepare | 897593 | Freeze the new protocol; GPU readiness for both seeds |
| Fits | 897594, array 0-7 | Exactly eight fixed-20-epoch detectors |
| Meta/heads | 897595, array 0-1 | Per-seed meta export and two logistic heads |
| Joint freeze | 897596 | Freeze both seed pipelines before new dev readouts |
| Dev predictions | 897597, array 0-1 | Per-seed 7,200-row development exports |
| Per-seed evaluation | 897598, array 0-1 | CPU paired source-image evaluation |
| Aggregate | 897599 | CPU primary 17/27 and descriptive 7/17/27 summaries |

Fit mapping:

| Array task | Seed | Arm | ViT layer |
| --- | ---: | --- | ---: |
| 897594_0 | 17 | block2 | 3 |
| 897594_1 | 17 | block5 | 6 |
| 897594_2 | 17 | block8 | 9 |
| 897594_3 | 17 | block11 | 12 |
| 897594_4 | 27 | block2 | 3 |
| 897594_5 | 27 | block5 | 6 |
| 897594_6 | 27 | block8 | 9 |
| 897594_7 | 27 | block11 | 12 |

At 07:35:59, the guardian was RUNNING on `rack-iscb-31` and prepare was RUNNING
on `s-004`, allocated an RTX 2080. All eight fits and downstream stages were
waiting on their declared dependencies. At 07:39:19, the new protocol had been
created and GPU readiness was underway; the guardian heartbeat remained live.
These observations do not claim that any detector had completed training.

At **07:43:02**, both GPU readiness checks had passed and **all eight fit-array
tasks were RUNNING simultaneously**, having started at approximately 07:41:20.
Tasks 0-1 were on `s-004`; tasks 2-7 were on `s-005`. Both are the declared
RTX 2080 resource class. Downstream stages remained dependency-pending.
Running allocations are not completed models or new scientific results.

Final reconciliation at **10:04:13** found an empty user queue and every
allocated task COMPLETED with exit `0:0`. All eight selected/resumable fits
were complete at exactly 20 epochs. Meta/heads finished by 09:44:20, the joint
freeze by 09:44:54, both dev exports by 10:01:51, both per-seed evaluations by
10:02:15, aggregation by 10:02:51, and guardian verification by 10:03:09.
The original seed-7 late status and source artifacts remained unchanged.

## Frozen resource limits and cutoffs

All times are Israel time:

- Base fits and audits: before **September 16, 18:00**.
- Meta/head/dev prediction stages: before **September 16, 23:00**.
- Evaluation and aggregate artifacts: before **September 17, 01:00**.
- Guardian reporting grace: 15 minutes after the evaluation cutoff.

Existing resource class only: account `gpu-students`, GPU partition
`studentbatch`, constraint `geforce_rtx_2080`, one GPU, six CPUs and 32000M per
GPU task. CPU stages use `cpu-killable`, two CPUs and 8000M; the guardian uses
one CPU. Maximum GPU concurrency is eight, subject to scheduler availability.

GPU allocation caps are prepare 60 minutes, eight fits at 300 minutes each,
two meta/head tasks at 90 minutes each, and two dev-prediction tasks at
90 minutes each: **2,820 GPU-minutes (47 GPU-hours)** within the explicitly
approved **3,000 GPU-minute (50 GPU-hour) ceiling**. These are reservation
caps, not runtime predictions. No outside GPU reservation was present at launch.

Joint freeze is capped at 10 CPU minutes, each evaluation task at 45 minutes,
aggregation at 45 minutes, and the independent guardian at 1,140 minutes.
The two sequential GPU readiness checks each have a 1,200-second internal limit.
No automatic scientific retry, deadline extension, subset ensemble, stronger
GPU, additional seed, held-out-test access or HF upload is authorized here.

## Monitoring and result interpretation

Read the control root's `manifests/guardian.json`, submission intents,
`manifests/launch.json`, Slurm accounting and eventual `manifests/terminal.json`.
Reconcile those records after a disconnect; do not infer non-submission from
a failed local SSH command. All execution/dependencies run server-side and
survive laptop closure.

The selected checkpoints are under
`seedN/runs/ARM/seedN`, with per-seed heads, predictions and evaluation beside
them. Final aggregate artifacts are under the scientific root's `evaluation`.
A scheduler success or isolated output file is not sufficient: completion
requires the full hash-bound inventory and all eight valid 20-epoch fits.

The primary comparison remains the mean seed-wise stack-minus-last-only AUROC
difference over 17/27. Seed 7 remains separately labeled late and descriptive
in the all-three-seed summary. This reused-development-data comparison cannot
establish GNN/topology necessity or independent-test generalization.
