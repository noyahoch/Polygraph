# September 13 layer screen — user-authorized execution rules

The user authorized this new study on university Slurm and plans to close the laptop. Root orchestrates; the experiment engineer owns this namespace and code; the assigned Slurm operator owns transfer, allocations, submission, dependencies and monitoring. All ML, tests, extraction and detector training run in Slurm. The Mac is used only for reading/writing code and SSH orchestration. No local GPU workload. On September 14 the user explicitly authorized small, meaningful local Git commits; remote Git pushes remain forbidden. Do not modify the prior frozen source, cache, protocol or results.

Use the identical frozen September 10 classifier and processor revisions and the prior 2,400 train / 800 validation source-photo groups with all nine conditions: 21,600 train and 7,200 validation records. Never evaluate the prior 800 test photos or fresh test photos. This is an exploratory development screen. Full final token hidden states (768 channels) are common to all arms; only attention layer exposure varies. Six representations: zero-based layers 2, 5, 8, 11, their four-layer union, and all twelve. Seeds 7 and 17 each; additional layer11 seeds1 and27 support a descriptive same-layer four-seed ensemble. All head identities remain explicit. Head values at or below .02 are zero, graph direction is key→query, and self-attention appears only in node diagonals. Coordinates have a common zero layer-position channel.

Every arm uses width64, two edge-gated mean layers, dropout.15, AdamW lr.002/wd.0001, batch24, minimum20 and maximum 60 epochs, patience8, selected by validation AUROC. Input projection parameter counts vary; do not claim matched capacity. Root may approve a uniform lower batch only after an allocated GPU memory preflight, before scientific training. No learned stacker, architecture/head/capacity search or classifier fine-tuning.

Extraction is chunked and restartable, saving one hidden tensor per image and sparse per-head values. Preflight uses a separate diagnostic cache. Full extraction is permitted only after storage/runtime review; detector jobs depend on a complete hash-verified cache. Each training epoch saves optimizer/model/RNG/sampler and history. Queue dependencies must survive laptop closure. Report actual Slurm IDs and durable paths; do not equate queued work with completed results.

The experiment-relative remote root is `experiments/layer_screen_20260913` under the existing Polygraph Slurm workspace; Ops owns the machine-specific location. Reuse the old read-only topology environment/data/HF artifacts; keep new releases, feature_cache and runs separate. The dated updates below supersede the initial preflight-pending state. No scientific training may start before reviewed runtime and budget admission.

## Recovery status — September 14, 2026, morning Israel time

Ops verified at 08:51:57 that Slurm is accessible and the user's queue is empty. Only the five original jobs existed and completed the previous evening around 19:50. All six model CUDA preflights passed; the existing diagnostic cache has 36 records in one 68,212,640-byte shard. The 75 files in immutable source release `1cc62d920705ba78` verified unchanged. No timing-correction profile ran overnight, and no scientific fits were submitted. Budget admission rejected the original extrapolation (approximately 2,430.75 GPU-hours versus the 64-hour cap).

The original extrapolation incorrectly repeated first-CUDA-step initialization and combined dataset-construction/first-shard overhead at recurring rates. Real recurring CPU graph reconstruction and shard deserialization costs still exist. The original preflight remains preserved; its `cold_shard` is not a pure disk-throughput measure. At morning audit, warmed timing code had not yet been saved.

`profile_timing.py` now implements the authorized technical correction: one Slurm allocation capped at 15 minutes by Ops, with an internal 720-second limit after imports. It uses the same diagnostic cache, all six representations, batch24, two warmup rounds and five measured rounds; it separates production loader verification/deserialization, materialization/collation, transfer, first/warmed training steps, evaluation-mode forward and disposable checkpoint serialization. All model mutations are disposable and create no scientific fit. The one-shard sample cannot establish multishard NFS throughput or justify assuming the full cache remains in RAM. Clearing the application's one-shard cache does not clear OS/NFS caches.

The independent source review passed. Ops submitted profiling job `891747` from immutable release `fa3df78e67cc5781`; it completed with exit `0:0` on September 14 at 08:59:44 Israel after approximately four minutes of allocation. All six technical timing profiles completed; the measured profiling phase took 51.4 seconds. These are engineering measurements, not new scientific model results. Ops confirmed the queue was empty and no production fits had been submitted after completion.

The revised estimate and subsequent failed worker diagnostic are recorded below. Production is not admitted. Preserve the original scientific matrix, input representations, maximum 60 epochs and 64 GPU-hour total cap, including diagnostic allocations. Do not launch fitting or change the scientific protocol merely to fit the cap. No local numerical tests or immutable source-release rewrites were performed during recovery. See the dated [engineering status report](../../docs/experiments/september13/ENGINEERING_STATUS_20260914.md) for evidence, reproduction and limits.

## Final bounded worker diagnostic — September 14, failed validation

Ops' corrected estimate still exceeds the cap: 73.23 GPU-hours for training alone at all 14 fits and maximum 60 epochs, 96.52 hours including validation/audits before I/O/startup/capture, and 196.5 hours with conservative allocation margins. Root authorized one final 15-minute technical diagnostic, with six CPUs and one GPU, to measure optional 2 and 4 loader workers. This is not permission to change the scientific matrix or continue indefinite optimization.

The new `loader_runtime.py` uses CUDA-safe spawn, CPU-only deterministic workers, FIFO delivery, prefetch 1, persistent training workers, and the unchanged production sampler/materialization. The default remains 0 workers. Dedicated loader-generator accounting consumes one draw per successful logical iterator, preserving its cursor across persistent-worker reuse versus epoch-boundary recreation; the diagnostic must verify this on the pinned Torch build. Dataset pickling clears the parent's resident shard cache. Each worker still pays its own checksum and one-shard LRU costs. Production validation retains its fresh-loader lifecycle, measured explicitly.

Independent static review passed and Ops submitted job `891833` from immutable release `deb4869e24cc32e7`. It ran 09:13:05–09:16:47 Israel on September 14, with 222 seconds of allocation, and **failed before completing its first case**. The diagnostic phase took 21.02 seconds. The result `manifests/profile_workers_20260914.json` has `passed=false`, `cases={}`, zero scientific fits and no test evaluation. Its source SHA256 is `bc3dba4163ec83e8aa457cb8c66120e344090f55e2850f3713caad2ef940b116`.

The failure was `block11/workers=0`: final model parameters after reload/replay of epoch 2 did not match uninterrupted epoch 2 within the fixture's 1e-5 absolute/relative tolerances. An unnamed one-element tensor differed by 0.0001257807. The immediately preceding exact input-hash and loss/gradient tolerance checks passed; subsequent final optimizer/sampler/RNG comparisons were not reached. No 2/4-worker case or throughput measurement ran, so worker correctness and speedup remain unvalidated.

Read-only engineering and independent review found no provable snapshot-aliasing or restore defect. Exact cause is unknown because the log lacks the parameter name and detailed states. CUDA reduction nondeterminism/near-zero-gradient AdamW sensitivity is plausible, not confirmed. The completed September 10 study and its prediction-restoration audit are separate and unchanged; this fixture tests the additional property of near-identical resumed parameter trajectories.

The intended throughput phase replays eight traversals of the same 36 rows, giving 288 presentations and 12 full batches; it was not reached. It would still not establish multishard NFS performance. Optional workers remain disabled by default. Graph materialization and the serial loader contract are unchanged; runtime configuration/source binding prevents silently resuming an older checkpoint with this new release.

**Restart boundary:** Ops verified the queue empty at 10:21 Israel, with no jobs since 891833. No full extraction or scientific fits are submitted. No automatic retry, source fix, tolerance relaxation, further optimization or new GPU diagnostic is authorized. Preserve the failure evidence and wait for a separate decision about investigating the missing parameter/state evidence or revising the feasible study scope. Do not claim a speedup or budget admission.

Only the new September 13 namespace changed. No local numerical test, new scientific fit, remote push or immutable-release rewrite occurred. Small coherent local commits are authorized to archive the as-executed opt-in runtime/diagnostic and, separately, its failed validation report; this does not certify the optimization. Preserve others' edits. Ops remains the sole scheduler operator.

## Renewed focused recovery authorization — September 14

After the failure report, the user explicitly asked to fix the failure and run again. This **supersedes the preceding stop on further diagnosis**, within a focused recovery scope. Root authorized an initial 15-minute Slurm allocation; Ops alone stages and submits it. No local ML/tests, scientific matrix change, automatic full-training admission or Git push is authorized. Continue through evidence-backed fixes and independent review, asking the user only when a material unresolved choice requires their input. Preserve the original failed source, gates and logs.

The separate `diagnose_resume.py` is ready for review. It examines only `block11`, seed 7, workers 0 and the same 36 diagnostic records. After epoch 1, a jointly deep-copied model/optimizer pair provides a same-checkpoint in-memory replay control without serialization. The original model continues epoch2. The copied pair, a disk checkpoint loaded into the already-used pair (matching 891833), and a disk checkpoint loaded into a fresh pair each replay epoch2 from the same sampler and loader/global RNG states. It verifies optimizer-to-cloned-parameter references and records immediate exact state comparisons. Named differences, per-step gradients/logits/loss/input IDs, fixed evaluation logits and all states are saved. Original1e-5 gates stay visible even when they fail; evidence collection does not stop at the first scalar mismatch.

The bounded job attempts ordinary CUDA and deterministic CUDA, with a diagnostic CPU reference only if deterministic CUDA is unavailable. Ops exports `CUBLAS_WORKSPACE_CONFIG=:4096:8` before Python; this and the instrumentation may affect numerical behavior, so they are recorded. One replay pair cannot establish a universal numerical-noise bound. No worker-speed measurement or parity pass is claimed before actual results. Durable outputs go to the new experiment-relative `manifests/resume_recovery_20260914/`; the earlier 891833 artifacts remain intact.

Next boundary: independent source review, then the sole Ops operator submits this one localization job. Interpret named results before changing production code, tolerances or exemptions. Any subsequent fix must address an evidenced cause and be reviewed before re-execution. Save reports and small coherent local commits as the recovery progresses.

## Localization result and corrected worker retry — September 14

Job `892060` completed with exit 0:0 at 10:35:56 Israel, approximately 131 seconds of allocation and 24.62 seconds of measured diagnosis, from immutable release `450b06809de915e8`. All three immediate starting states were exact. Under ordinary CUDA, the only final leaf failing tolerance was `model.gate.2.bias`, including the GPU in-memory replay with no save/load. Fixed evaluation logits differed by at most 2.3841858e-7; original inputs/loss/gradient gates passed. Under deterministic CUDA, all three routes had exact final states, gradients and logits. This supports CUDA numerical repeatability as the cause of the assertion rather than checkpoint corruption, within this diagnostic's limited scope. The recorded bias adds a common constant before softmax and cancels in exact arithmetic; no parameter is removed or exempted.

Root authorized a further single 15-minute/6CPU Slurm worker retry. The focused fix enables deterministic algorithms only for the unchanged 1e-5 parity/resume audit, restores backend flags and parent RNG afterward, and measures separate ordinary-CUDA throughput on fresh disposable models. `CUBLAS_WORKSPACE_CONFIG=:4096:8` must be exported before Python for both this benchmark and any future production wrapper. New training configurations bind deterministic flags and CUBLAS environment; the existing architecture, `_seed` policy, saved-prediction audit, materialization and scientific matrix stay unchanged.

Cases now preserve failed audit outcomes and may still record independent timing, but overall validation requires all 18 cases to pass and full training remains subject to reviewed budget admission. Output is a new `manifests/profile_workers_recovery_20260914.json`; earlier sources/logs are preserved. Independent review passed, and Ops submitted job 892080 at 10:40:46 Israel from immutable release `7702e1de15063208`, initially pending, with one GPU/six CPUs and a 15-minute cap. Next boundary is actual audit/timing results followed by independent interpretation and resource admission review. No production fit or measured worker speedup is claimed at this checkpoint.

## Five cases complete; selected continuation — September 14

Job 892080 became terminal with Slurm TIMEOUT at 10:55:48 Israel, using 902 seconds of allocation. Five complete cases passed every audit and recorded production-mode timing: block11/workers0,2,4 and union12/workers0,2. The overall 18-case attempt is incomplete, not a failed numerical audit. Parent JSON SHA256 is `fee534900794bdef95f4f4c3c4ae0d4a93b21d42a8d1aab51d433f321395f3ca`. The 13 missing cases are workers0/2/4 for block2, block5, block8 and union4, plus union12/workers4. Preserve all five completed measurements.

Root authorized up to five independent 20-minute/6CPU continuation jobs, at most five concurrently and below the global eight-GPU limit. `continue_workers.py` adds only selection, compatibility checks, per-case persistence and deadlines; all 11 core source files, measurement helpers, model/audit math, trainer, loader, materialization and protocol remain byte-identical. Independent review passed at wrapper SHA256 `ea332a9cbc628ceae841c343443f9d89e1f3f2eeea6c0d270fc7f525013b86a1`.

Ops froze `ops/frozen_missing_cases_892080.json` only after the parent became terminal, SHA256 `0df1e6e934cab5a6944523ecbc6dae125d485ef76aa4b29f014e841a509a45b7`. The wrapper verifies that partition plus parent/core/cache/runtime/Torch/GPU/6CPU identity. Select only each arm's missing worker counts. A missing worker comparison may regenerate its workers0 deterministic audit reference because the old JSON has no gradient/input tensors; it must not repeat or replace the old workers0 throughput. The cost is recorded separately.

Each case is saved independently with compatibility and job/source lineage. Fully completed compatible case files are reused on restart; failed attempts remain archived. Use an 840-second internal deadline inside the 20-minute allocation to allow imports and cleanup. Ops owns submissions and the merge. Acceptance requires all 18 cases exactly once, no incompatible or conflicting duplicates, all audits/timings passing, and complete accounting of startup, reference and allocation costs. Selected-job success is not full coverage or permission for 14 scientific fits. No validation-lifecycle optimization is authorized here.
